# 核心引脚分配表（STM32F411CEU6）

> 主控已选型：STM32F411CEU6（配置源：根目录 `FreeRTOS_Time.ioc`，2026-07-13 核对）。
> 未填行随模块设计逐行补齐，并与 CubeMX 配置保持一致。
> 原则：引脚分配一旦确定，是各 `xxx_port/` 与 `03_Config/board_*_map.h` 板级映射的**唯一依据**。

| 功能 | 器件 | 引脚 | 复用/外设 | 方向 | 备注 |
|---|---|---|---|---|---|
| 电机 PWM A | 电机驱动板（TB6612 类，型号待定） | PA9 | TIM1_CH2 | 输出 | ioc 标签 `PA9_PWMA_TIM1`；ARR=10000（万分比占空比） |
| 电机 PWM B | 同上 | PA8 | TIM1_CH1 | 输出 | ioc 标签 `PA8_PWMB_TIM1` |
| 电机方向 | 同上 | PB4/PB5、PB6/PB7 | GPIO 输出 | 输出 | `AIN1/AIN2`、`BIN1/BIN2`：TB6612 型 IN1/IN2 双线方向（注意：电机模块输出拓扑需在 DUAL_PWM/PWM_DIR 之外补 IN1/IN2 策略） |
| 编码器 A |  |  |  | 输入 | TODO：ioc 未配置；建议优先 TIM2/TIM5（32 位计数器） |
| 编码器 B |  |  |  | 输入 | TODO：同上 |
| 灰度/循迹 ×8 | 8 路二值灰度探头（型号待定） | PA0–PA7 | GPIO 输入（V1 直读 IDR） | 输入 | ioc 标签 `PA0_LSENSOR_0`…`PA7_LSENSOR_7`；**bit i = PAi = 槽位 i**；有效电平待实测；V2 若上 DMA 受 TIM1_UP+DMA2 约束（见《灰度传感器模块设计》9.1） |
| 蓝牙串口 | BT 模块 | PA11(TX)/PA12(RX) | USART6 | 收发 | ioc 标签 `PA11_BT_UART_TX`/`PA12_BT_UART_RX` |
| IMU 串口 | IMU（串口输出型） | PA15(TX)/PB3(RX) | USART1 | 收发 | ioc 标签 `PA15_IMU_UART_TX`/`PB3_IMU_UART_RX` |
| ADC 采样 | 待确认（电池电压？） | PB0 | ADC1_IN8 | 输入 | 用途待登记 |
| SPI2 | 待确认 | PB10(SCK)/PB14(MISO)/PB15(MOSI) | SPI2 全双工主 | 收发 | 用途与片选引脚待登记 |
| 调试口/RTT | J-Link | PA13(SWDIO)/PA14(SWCLK) | SWD | 收发 | 日志走 SEGGER RTT（SWD 通道），无独立调试串口 |
| 状态 LED |  |  |  | 输出 | TODO：ioc 未配置 |
| 按键 |  |  |  | 输入 | TODO：ioc 未配置 |
| 晶振 | HSE | PH0/PH1 | RCC OSC_IN/OUT | — | 外部晶振 |
