# 传感器数据模型（占位）

> 对应课程：第6课·数据模型（cfg / ctx / data / ops）。循迹传感方案未定，先留骨架。

## 数据模型四元（预留）

| 元 | 含义 | 示例（循迹传感） |
|---|---|---|
| `cfg`  | 静态配置 | 通道数、阈值、采样周期 |
| `ctx`  | 运行上下文 | 滤波器状态、上一次采样 |
| `data` | 采样结果 | 各通道灰度值 / 归一化位置偏差 |
| `ops`  | 操作集 | init / sample / get_position |

## 待定

- [ ] 循迹方案：灰度阵列 / 摄像头 / 电磁？
- [ ] 输出量定义：位置偏差范围、更新频率
