# 日志规范（占位）

> 建议后端：SEGGER RTT 彩色分级日志 + 每模块宏开关（关闭时零开销）。
> 落点：接口在 `03_Platform/platform_middleware/platform_log.h`（待建），实现在 `04_Impl/impl_middleware/impl_log`。

## 分级（预留）

| 级别 | 宏 | 用途 |
|---|---|---|
| Debug | `LOGD` | 高频调试细节 |
| Info  | `LOGI` | 关键流程节点 |
| Warn  | `LOGW` | 可恢复异常 |
| Error | `LOGE` | 错误 / 故障 |

## 约定（待定）

- [ ] 每模块一个 tag 与独立开关
- [ ] ISR / 高频路径慎用日志
- [ ] 关闭开关时宏展开为空，零开销
