# 02_Middleware_Platform 中间件平台层

## 📌 模块定位
封装通用中间件 / 协议栈 / 库（Shell、环形缓冲、协议解析、参数存储等），提升可移植性与复用能力。（循迹小车暂无 LVGL 等图形库需求。）

## 📁 典型模块（按需新建）
- 命令行 Shell（LetterShell）
- 通信协议帧解析
- 参数 / 标定存储

## ✅ 命名规范
- 库：`MW_Lib_模块名`；接口：`MW_If_接口名`

## 🔄 依赖关系
- 依赖 `02_OS_Platform` 提供的任务 / 内存接口；可选依赖 BSP / MCU 能力。
